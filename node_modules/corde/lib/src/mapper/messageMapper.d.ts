import { MessageEmbed } from "discord.js";
import { IMessageEmbed } from "../types";
export declare namespace mapper {
  function embedInterfaceToMessageEmbed(embedLike: IMessageEmbed): MessageEmbed;
  function messageEmbedToMessageEmbedInterface(message: MessageEmbed): IMessageEmbed;
}
