import { IRoleIdentifier, ITestReport, RolePermission } from "../../../types";
import { IExpectTestBaseParams } from "../../../types";
import { ExpectTest } from "../expectTest";
/**
 * @internal
 */
export declare class ToSetRolePermission extends ExpectTest {
  constructor(params: IExpectTestBaseParams);
  action(
    roleIdentifier: string | IRoleIdentifier,
    permissions: RolePermission[],
  ): Promise<ITestReport>;
}
